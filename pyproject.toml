[project]
name = "pokedexml"
version = "0.0.0"
requires-python = ">=3.12,<3.14"
dependencies = [
    "keras",
    "scikit-learn",
    "matplotlib",
    "numpy",
    "opencv-python",
    "tensorflow",
    "imutils",
]

[tool.uv]
package = false
